apiVersion: v1
kind: Service
metadata:
  name: {{ include "iceci.fullname" . }}-ui
  labels:
    {{- include "iceci.labels" . | nindent 4 }}
spec:
  type: {{ .Values.ui.service.type }}
  ports:
    - port: {{ .Values.ui.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "iceci.selectorLabels" . | nindent 4 }}
